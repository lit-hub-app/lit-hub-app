import styles from '@/styles/components/core/Footer.module.scss';

export default function FooterComponent() {

  return (
    <footer>
      <div className={styles.footerHeader}>
        <h2>FOOTER</h2>
      </div>
    </footer>
  )
};
